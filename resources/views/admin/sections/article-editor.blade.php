<div class="page-heading"><div><div class="eyebrow">{{ $article ? 'Edit article' : 'New article' }}</div><h1>{{ $article?->title ?? 'Create an article' }}</h1><p>Manage the reading content and the quiz members must complete.</p></div><a class="mini-btn" href="{{ url('/admin/articles') }}">← Back to articles</a></div>
<div class="quiz-editor-layout"><main class="card panel"><form class="admin-form quiz-editor-form" method="POST" action="{{ $article ? route('admin.articles.update',$article) : route('admin.articles.store') }}" data-article-form>@csrf @if($article) @method('PATCH') @endif @include('admin.sections.article-form')</form></main><aside class="card panel quiz-editor-help"><h2>Publishing guide</h2><p>Draft articles stay hidden. Published articles are returned to members in the order you set.</p><div class="quiz-community-counts">@foreach($articleCommunities as $community)<div><span>{{ $community->name }}</span><strong>{{ $community->learning_articles_count }} existing</strong></div>@endforeach</div></aside></div>
