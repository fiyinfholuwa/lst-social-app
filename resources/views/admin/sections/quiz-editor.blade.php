<div class="page-heading"><div><div class="eyebrow">{{ $quiz ? 'Edit quiz' : 'New quiz' }}</div><h1>{{ $quiz ? $quiz->title : 'Create a quiz' }}</h1><p>{{ $quiz ? 'Update the quiz content, questions and publishing settings.' : 'Choose a community, add the article instructions and build the questions.' }}</p></div><a class="mini-btn" href="{{ url('/admin/quizzes') }}">← Back to quizzes</a></div>
<div class="quiz-editor-layout"><main class="card panel"><form class="admin-form quiz-editor-form" method="POST" action="{{ $quiz ? route('admin.quizzes.update',$quiz) : route('admin.quizzes.store') }}" data-quiz-form>@csrf @if($quiz) @method('PATCH') @endif @include('admin.sections.quiz-form', ['quiz'=>$quiz])</form></main><aside class="card panel quiz-editor-help"><h2>Community guide</h2><p>Select the community whose members should receive this quiz.</p><div class="quiz-community-counts">@foreach($quizCommunities as $community)<div><span>{{ $community->name }}</span><strong>{{ $community->quizzes_count }} existing</strong></div>@endforeach</div><p class="panel-sub">You can create multiple quizzes for the same community as long as each title is unique.</p></aside></div>
